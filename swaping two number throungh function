
#include <stdio.h>
void swap(int *a,int *b);
void main()
{
    int a=9,b=1;
    swap(&a,&b);
    printf("a=%d  b=%d",a,b);
}
void swap(int *a, int *b)
{
    int temp;
    temp=*a;
    *a=*b;
    *b=temp;
}
