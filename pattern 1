#include<stdio.h>
void main()
{
    int a,i,j,c=1;
    printf("enter the number");
    scanf("%d",&a);
    for(i=1;i<=a;i++)
    {
        for(j=1;j<=i;j++,c++)
        {
            if(c<=a)
            printf("%d ",c);
        }
        printf("\n");
    }
}
